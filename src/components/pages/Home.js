import React from 'react';
// import { Header, Grid, Divider, Container } from 'semantic-ui-react';
import Video from '../ui/Video';
import MenuHome from '../ui/MenuHome';

const Home = () => (
  <React.Fragment>
    <Video />
    <MenuHome />
  </React.Fragment>
);

export default Home;
